import React from "react";
import "../App.css";

function ColorList({ item, i }) {
  {
    item.map((row, i) => (
      <ul>
        <li>bologna</li>
      </ul>
    ));
  }
}

export default ColorList;
